<Window x:Class="EasySaveGUI.FormatLog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Options" Height="300" Width="400">
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Style>
    <Grid>
        <Label Content= "{DynamicResource StrLogFormat}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" FontSize="18"/>
        <RadioButton Content="JSON" x:Name="rbJson" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="50,80,0,0"/>
        <RadioButton Content="XML" x:Name="rbXml" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="50,110,0,0"/>
        <Button Content="Save" Click="btnSave_Click" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,180,0,0" Width="80"/>
    </Grid>
</Window>
